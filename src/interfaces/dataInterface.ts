export interface data {
    body:string;
    id: number;
    title:string;
    userId:number;
}
export interface comments {
    postId: number
    id: number
    name: string
    email: string
    body: string
}